// method = `DELETE`; the type of request: DELETE
// url = `http://localhost:3000/api/`; the server (file) location
// async = true; true (asynchronous) or false (asynchronous)

var together = {
    method = `POST`,
    url = `http://localhost:3000/api/`,
    async = true
};

module.exports = together;
